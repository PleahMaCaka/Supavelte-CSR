import type { CapacitorConfig } from "@capacitor/cli"

const config: CapacitorConfig = {
    appId: "kr.freshtech.app",
    appName: "Supavelte",
    webDir: "build"
}

// noinspection JSUnusedGlobalSymbols
export default config
